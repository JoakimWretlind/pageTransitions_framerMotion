import styled from 'styled-components';

export const MainHeader = styled.h1`
    color: #fafafa;
    text-transform: uppercase;
    font-weight: 900;
    font-size: clamp(3rem, 8vw, 14rem);
    letter-spacing: clamp(.3rem, 2vw, 1.2rem);
`;